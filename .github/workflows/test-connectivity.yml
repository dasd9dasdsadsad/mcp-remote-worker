name: Test Connectivity

on:
  workflow_dispatch:

jobs:
  test-connection:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: 🔍 Test NATS Connection
        run: |
          echo "Testing NATS connection to 165.232.134.47:4222..."
          nc -zv 165.232.134.47 4222 && echo "✅ NATS reachable" || echo "❌ NATS unreachable"
          
      - name: 🔍 Test PostgreSQL Connection
        run: |
          echo "Testing PostgreSQL connection to 165.232.134.47:5432..."
          nc -zv 165.232.134.47 5432 && echo "✅ PostgreSQL reachable" || echo "❌ PostgreSQL unreachable"
          
      - name: 🔍 Test Redis Connection
        run: |
          echo "Testing Redis connection to 165.232.134.47:6379..."
          nc -zv 165.232.134.47 6379 && echo "✅ Redis reachable" || echo "❌ Redis unreachable"

